//import {range, filter, chain, reduce, keys} from 'lodash-es'

export const greet = (name: string): string => {
  return `Hello, ${name}!`;
};


export const entryPoint = (): undefined => {
    throw new Error("defauilt export not implemented")
}
